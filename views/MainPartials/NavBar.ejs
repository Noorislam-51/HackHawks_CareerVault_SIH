<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<style>
  body {
    padding: 0;
    margin: 0;
    /* optional but recommended to remove default margin as well */
  }

  .header {
    background: white;
    padding: 1rem 2rem;
    border-bottom: 1px solid #e9ecef;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .header h1 {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 1.5rem;
    font-weight: 600;
    margin: 0;
  }

  .header .icon {
    color: #28a745;
    font-size: 1.5rem;
  }
</style>

<body>
  <header class="header">
    <h1>
      <span class="icon">
        <% if(userType==='staff' ) { %>
          👥
          <% } else if(userType==='student' ) { %>
            🎓
            <% } %>
      </span>
      <% if(userType==='staff' ) { %>
        Staff Dashboard
        <% } else if(userType==='student' ) { %>
          Student Dashboard
          <% } %>
    </h1>
    <div class="buttons">
      <% if(userType==='staff' ) { %>
        <a href="/staff-portal" class="btn">Staff Portal</a>
        <% } else if(userType==='student' ) { %>
          <a href="/student-portal" class="btn">Student Portal</a>
          <% } %>
            <a href="/logout" class="btn logout-btn">Logout</a>
    </div>
  </header>

</body>

</html>