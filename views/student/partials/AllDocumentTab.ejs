<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Documents</title>

  <style>
   

    h2 {
      font-weight: 700;
      color: #222;
      margin-bottom: 20px;
    }

    .document-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 20px;
    }

    .document-card {
    background: #fff;
    border-radius: 12px;
    padding: 20px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
    display: flex;
    flex-direction: column;
    min-height: 260px; /* 👈 Increased Height */
  }

    .document-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 18px rgba(0, 0, 0, 0.15);
    }

    .card-title {
      font-size: 18px;
      font-weight: 600;
      margin-bottom: 10px;
      color: #333;
    }

    .badge {
      display: inline-block;
      padding: 6px 10px;
      border-radius: 6px;
      font-size: 12px;
      font-weight: bold;
    }

    .badge.pending {
      background: #ffe08a;
      color: #8a6d3b;
    }

    .badge.approved {
      background: #c8f7c5;
      color: #2e7d32;
    }

    .badge.rejected {
      background: #f8cccc;
      color: #b71c1c;
    }

    .btn-view {
      margin-top: auto;
      text-align: center;
      background: #007bff;
      color: #fff;
      padding: 10px;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 500;
      transition: background 0.2s ease;
    }

    .btn-view:hover {
      background: #0056b3;
    }

    .alert {
      background: #e3f2fd;
      padding: 15px;
      border-radius: 8px;
      color: #0b5394;
    }
  </style>
</head>

<body>

  <div class="content-section tab-content" id="all-documents">
    <h2>📂 All Documents for <%= student.basicDetails.fullName %></h2>

    <% if (!allDocs || allDocs.length===0) { %>
      <div class="alert">
        You have not uploaded any documents yet.
      </div>
    <% } else { %>
      <div class="document-grid">
        <% allDocs.forEach(doc=> { %>
          <div class="document-card">
            <div class="card-title"><%= doc.title %></div>
            <p><strong>Type:</strong> <%= doc.type %></p>
            <p><strong>Status:</strong>
              <% if (doc.status==="pending" ) { %>
                <span class="badge pending">Pending</span>
              <% } else if (doc.status==="approved" ) { %>
                <span class="badge approved">Approved</span>
              <% } else if (doc.status==="rejected" ) { %>
                <span class="badge rejected">Rejected</span>
              <% } %>
            </p>
            <a href="<%= doc.file_url %>" target="_blank" class="btn-view">View Document</a>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>

</body>
</html>
