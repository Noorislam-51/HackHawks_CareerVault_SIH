<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Documents</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/stylesheets/studentDashboard.css">
</head>
<div class="content-section tab-content" id="all-documents">

  <div class="container my-4 ">
    <h2 class="mb-4">📂 All Documents for <%= student.basicDetails.fullName %>
    </h2>

    <% if (!allDocs || allDocs.length===0) { %>
      <div class="alert alert-info" role="alert">
        You have not uploaded any documents yet.
      </div>
      <% } else { %>
        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
          <% allDocs.forEach(doc=> { %>
            <div class="col">
              <div class="card shadow-sm h-100 border-0">
                <div class="card-body d-flex flex-column">
                  <h5 class="card-title">
                    <%= doc.title %>
                  </h5>
                  <p class="card-text mb-2"><strong>Type:</strong>
                    <%= doc.type %>
                  </p>
                  <p class="card-text mb-3">
                    <strong>Status:</strong>
                    <% if (doc.status==="pending" ) { %>
                      <span class="badge bg-warning text-dark">Pending</span>
                      <% } else if (doc.status==="approved" ) { %>
                        <span class="badge bg-success">Approved</span>
                        <% } else if (doc.status==="rejected" ) { %>
                          <span class="badge bg-danger">Rejected</span>
                          <% } %>
                  </p>
                  <a href="<%= doc.file_url %>" target="_blank" class="btn btn-primary mt-auto">View Document</a>
                </div>
              </div>
            </div>
            <% }) %>
        </div>
        <% } %>
  </div>
</div>
</body>

</html>