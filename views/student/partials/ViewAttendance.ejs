<!DOCTYPE html>
<html>

<head>
  <title>Attendance Upload Result</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>

<body class="p-5">
  <div class="content-section tab-content  " id="view-attendance" ></div>
  <div class="container" >
    <h2 class="mb-4">✅ Attendance Processed</h2>

    <% if (typeof results !=='undefined' ) { %>
      <p><strong>Records Updated:</strong>
        <%= results.updated %>
      </p>

      <% if (results.notFound && results.notFound.length> 0) { %>
        <div class="alert alert-warning">
          <strong>Not Found Student IDs:</strong>
          <%= results.notFound.join(", ") %>
      </div>
    <% } %>
  <% } else { %>
    <p>No attendance data available yet.</p>
  <% } %>
</div>
</body>
</html>