<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Student & Staff Portal</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .option-circle {
      width: 200px;
      height: 200px;
      border-radius: 50%;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      font-size: 1.2rem;
      font-weight: 600;
      color: #fff;
      cursor: pointer;
      transition: 0.3s ease-in-out;
    }

    .option-circle:hover {
      transform: scale(1.1);
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
    }

    .student {
      background: #0d6efd;
      /* Bootstrap primary */
    }

    .staff {
      background: #198754;
      /* Bootstrap success */
    }
  </style>
</head>

<body class="bg-light d-flex justify-content-center align-items-center vh-100">

  <div class="container text-center">
    <h1 class="mb-5">Welcome to Smart Student Hub</h1>
    <div class="row justify-content-center g-5">
      <!-- Student Circle -->
      <div class="col-md-4 d-flex justify-content-center">
        <a href="/login/student" class="text-decoration-none">
          <div class="option-circle student">
            <span>🎓</span>
            <span>Student</span>
          </div>
        </a>
      </div>

      <!-- Staff Circle -->
      <div class="col-md-4 d-flex justify-content-center">
        <a href="/login/staff" class="text-decoration-none">
          <div class="option-circle staff">
            <span>👩‍🏫</span>
            <span>Staff</span>
          </div>
        </a>
      </div>

    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>



</body>

</html>